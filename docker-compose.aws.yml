# AWS S3 — run after: npm run setup (creates .env with your AWS keys)
#   docker compose -f docker-compose.aws.yml up --build
services:
  app:
    build: .
    ports:
      - "0.0.0.0:3000:3000"
    env_file:
      - .env
    volumes:
      - app_data:/app/data

volumes:
  app_data:
